[project]
name = "ha-sacn-control"
version = "0.1.0"
description = "Home Assistant HACS integration for bidirectional sACN (E1.31) lighting control"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
